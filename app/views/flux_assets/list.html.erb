<ol id="selectable">
<% @assets.each do |asset| %>
  <% next unless asset.public_url %>
	<li class="ui-state-default"><img src="http://thumbs.fluxiom.com/<%= asset.thumb_url%>" data-public-url="<%= asset.public_url %>" data-description="<%= asset.description %>"></img></li>
<% end %>	  
</ol>
	  